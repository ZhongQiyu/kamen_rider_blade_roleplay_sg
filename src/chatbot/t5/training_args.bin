import pickle

training_args = {
    "learning_rate": 5e-5,
    "num_train_epochs": 3,
    "per_device_train_batch_size": 8,
    "save_steps": 10_000,
    "save_total_limit": 2,
    "evaluation_strategy": "epoch",
    "logging_dir": "./logs",
}

with open("training_args.bin", "wb") as f:
    pickle.dump(training_args, f)
